/*
    index.js
    This file is ran when the extension's icon is clicked. For now, it 
    does not contain much functionality.
*/


chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
    // TODO: Receive trace messages from injected content script and
    // display them in the extension's popup window.
});